name: release

# Managed by maelys-release v0.33.0 (0.33.0). Regenerate with
# 'maelys-release adopt' of maelys-release; do not edit by hand.
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: Existing signed tag whose release and Homebrew publication are replayed
        required: true
        type: string

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  release:
    uses: maelys-dev/maelys-release/.github/workflows/release.yml@15d4bac77af504dbe746ede23e1fe11612aab06a # v0.33.0
    permissions:
      contents: write
      id-token: write
      attestations: write
    with:
      product: maelys-oci
      tag: ${{ inputs.tag || github.ref_name }}
      dependency_checkout: |
        sh scripts/checkout-dependency.sh maelys-cli
        sh scripts/checkout-dependency.sh maelys-http
        sh scripts/checkout-dependency.sh maelys-json
        sh scripts/checkout-dependency.sh maelys-system
      linux_packages: build-essential dpkg-dev file rpm pkg-config python3 perl libarchive-dev libext2fs-dev comerr-dev e2fsprogs libmbedtls-dev
      macos_packages: pkgconf libarchive e2fsprogs mbedtls python3
