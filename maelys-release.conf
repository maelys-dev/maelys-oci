# Read by maelys-release (docs/conventions.md of maelys-release).

# The build reads its pinned dependencies under MAELYS_DEPENDENCIES_DIR, the
# root the socle materialises and gives (maelys-release dependencies . --apply
# on a machine, scripts/checkout-dependencies.sh in a job), never beside
# the repository: ../NAME is whatever sits there.
[dependencies]
apart
